export interface TranslationKeys {
  common: {
    accept: string;
    cancel: string;
    save: string;
    delete: string;
  };
  tabs: {
    home: string;
    projects: string;
    calendar: string;
    data: string;
  };
}
